# Redirecionamento HTTP para HTTPS
server {
    listen 80;
    server_name canela-stream.com.br www.canela-stream.com.br;
    return 301 https://canela-stream.com.br$request_uri;
}

server {
    listen 80;
    server_name torrent.canela-stream.com.br;
    return 301 https://torrent.canela-stream.com.br$request_uri;
}

server {
    listen 80;
    server_name jackett.canela-stream.com.br;
    return 301 https://jackett.canela-stream.com.br$request_uri;
}

server {
    listen 80;
    server_name openvpn.canela-stream.com.br;
    return 301 https://openvpn.canela-stream.com.br$request_uri;
}

# Configuração HTTPS para Jellyfin
server {
    listen 443 ssl;
    http2 on;
    server_name canela-stream.com.br www.canela-stream.com.br;

    ssl_certificate /etc/nginx/certs/cloudflare.pem;
    ssl_certificate_key /etc/nginx/certs/cloudflare.key;

    location / {
        proxy_pass http://192.168.100.10:8096;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $http_connection;

        # Configurações específicas para streaming
        proxy_buffering off;  # Desabilita buffering para streaming
        proxy_request_buffering off;
        client_max_body_size 10G;  # Permitir grandes uploads
    }

    location ~* \.(mp4|mkv|webm|jpg|jpeg|png|gif)$ {
        proxy_pass http://192.168.100.10:8096;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_no_cache 1;
        proxy_cache_bypass 1;

        proxy_set_header Range $http_range;
        proxy_set_header If-Range $http_if_range;
        add_header X-Cache-Status $upstream_cache_status;
    }

    error_log /var/log/nginx/jellyfin-error.log warn;
    access_log /var/log/nginx/jellyfin-access.log;
}

# Configuração HTTPS para Transmission
server {
    listen 443 ssl;
    http2 on;
    server_name torrent.canela-stream.com.br;

    ssl_certificate /etc/nginx/certs/cloudflare.pem;
    ssl_certificate_key /etc/nginx/certs/cloudflare.key;

    location / {
        proxy_pass http://192.168.100.13:9091;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_cache transmission_cache;
        proxy_cache_valid 200 302 10m;
        proxy_cache_valid 404 1m;
        add_header X-Cache-Status $upstream_cache_status;
    }

    error_log /var/log/nginx/transmission-error.log warn;
    access_log /var/log/nginx/transmission-access.log;
}

# Configuração HTTPS para Jackett
server {
    listen 443 ssl;
    http2 on;
    server_name jackett.canela-stream.com.br;

    ssl_certificate /etc/nginx/certs/cloudflare.pem;
    ssl_certificate_key /etc/nginx/certs/cloudflare.key;

    location / {
        proxy_pass http://192.168.100.15:9117;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_cache jackett_cache;
        proxy_cache_valid 200 302 10m;
        proxy_cache_valid 404 1m;
        add_header X-Cache-Status $upstream_cache_status;
    }

    error_log /var/log/nginx/jackett-error.log warn;
    access_log /var/log/nginx/jackett-access.log;
}

# Configuração HTTPS para OpenVPN
server {
    listen 443 ssl;
    http2 on;
    server_name openvpn.canela-stream.com.br;

    ssl_certificate /etc/nginx/certs/cloudflare.pem;
    ssl_certificate_key /etc/nginx/certs/cloudflare.key;

    location / {
        proxy_pass https://192.168.100.14:943;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_cache openvpn_cache;
        proxy_cache_valid 200 302 10m;
        proxy_cache_valid 404 1m;
        add_header X-Cache-Status $upstream_cache_status;
    }

    error_log /var/log/nginx/openvpn-error.log warn;
    access_log /var/log/nginx/openvpn-access.log;
}